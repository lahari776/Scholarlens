ScholarLens (Setup)
1. Install & Run
npm.cmd install
npm.cmd start
2. Create .env

Create a .env file in the root folder and add:

MONGO_URI= (ask me)
JWT_SECRET=anything
PORT=5000
3. MongoDB

We’re using MongoDB Atlas
I’ll share the MONGO_URI — just paste it, no setup needed.

Notes
.env is not included in repo (you have to create it)
Don’t push .env
If it doesn’t work → ping me 👍
Project is still in progress, things might break 😄
